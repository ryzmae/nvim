return {
  "wakatime/vim-wakatime",
  lazy = false, -- Load immediately since it's an essential tracking plugin
  config = function()
    -- You can add any specific WakaTime settings here if needed,
    -- but for basic setup, it often works out of the box.
  end,
}
